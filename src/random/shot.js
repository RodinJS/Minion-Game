import * as R from 'rodin/core';

export const shot = ( evt, finalPos, callBack ) => {
    const bullet = new R.Sphere();
    // bullet.animatieTO(finalPos);
    // onanimationfinish
    //{
        //remove bullet
    //   callBack && callBack();
    //}

};